## 1. 恢复变更与建立真实失败基线

- [x] 1.1 恢复被错误归档的 `align-ui-with-approved-renderings`，重新打开并认领 Beads issue
- [x] 1.2 审阅用户提供的 11 张真实 DMG 截图，并在当前 v0.3.0 桌面应用中实际触达工作台、任务中心、AI、模型和设置页
- [x] 1.3 建立逐控件验收矩阵，覆盖每个可见页签、按钮、筛选、下拉框、开关、播放器、seek、下载与破坏性确认入口

## 2. 首页与任务结果

- [x] 2.1 修正首页参数控件对齐、`min-width` 和右侧安全间距，保证开始按钮在维护视口中完整可见
- [x] 2.2 将首页转写结果恢复为固定高度内部滚动窗口，只保留 SRT/TXT 下载并验证长文本
- [x] 2.3 触达首页后端、模型、语言、更多设置、文件选择、开始、最近任务详情/下载/打开位置和播放器控件
- [x] 2.4 统一首页主内容卡四角圆角，移除模型字段下无助于选择的设备实现提示，并在最终 DMG 验证

## 3. 任务中心

- [x] 3.1 统一三栏背景、边框、尺寸与滚动所有权，修复右侧状态区裁切和空态窄列
- [x] 3.2 让最近任务、进行中、已完成筛选真正切换列表、计数和选中态，并覆盖搜索与任务切换
- [x] 3.3 删除无行为的头部检索、重复播放、更多和关闭按钮；移除分段编辑页签、字幕预览与分段表单
- [x] 3.4 修复播放器加载、播放/暂停、seek、音量与转写段落联动；转写结果使用固定高度内部滚动，并覆盖冻结 macOS 后端的外置卷 Range 播放
- [x] 3.5 触达右侧复制、SRT、TXT、打开位置和 AI 字幕校对入口，确认长 ID/长文件名不裁切主要操作

## 4. AI 工作台

- [x] 4.1 统一三页签选中态、三栏宽度、滚动所有权和错误/进行中/完成状态
- [x] 4.2 修复字幕校对的模型选择、开始、超时诊断、重试和结果展示
- [x] 4.3 恢复字幕翻译的提供商、源语言、目标语言、字幕版本、开始/重试和导出操作，并保证首屏/内部滚动可达
- [x] 4.4 恢复对话的字幕任务选择与“不绑定”切换，验证新建会话、历史切换、发送和回答依据
- [x] 4.5 修复本地 Ollama 长字幕校对固定 90 秒超时：使用本地批次/期限并在超时、截断、上下文或结构错误时自动拆分
- [x] 4.6 加速长字幕翻译：提高安全首轮批量、改用紧凑有序响应并保留缺段/错位检测与自动拆分回退
- [x] 4.7 修复 AI 工作台无限下拉：AppShell 与三栏显式约束高度，任务、结果和状态只在各自区域内部滚动
- [x] 4.8 校对运行持久化当前界面语言，并让首轮与拆分请求的修改原因始终匹配中文/英文界面
- [x] 4.9 为每条校对意见增加源字幕片段试听，按时间线起点播放并在该句终点自动停止、支持从片段起点重播
- [x] 4.10 基于真实 Ollama 指标完成性能优化与回归：校对使用紧凑三元组、翻译使用 64 段/6000 字符首轮批次，UI 明确区分排队和推理且不增加无效并发

## 5. 模型与设置

- [x] 5.1 将模型管理重构为紧凑行式列表，校准筛选、搜索、刷新、展开天梯、详情、下载和设为默认
- [x] 5.2 验证默认模型在模型页、设置页和首页联动，且 Ollama、Qwen、OpenAI 等品牌图标映射正确
- [x] 5.3 统一设置页签选中态和内容密度，修复转写默认值中的 Switch 被拉伸问题
- [x] 5.4 逐页触达通用、转写默认值、显卡加速、线上平台、AI LLM 提供商、存储与诊断、关于中的所有非破坏性控件；变更设置后恢复原值
- [x] 5.5 让所有共享 Switch 在开启时使用当前强调色、关闭时使用中性底色，并验证切换强调色后仍正确联动
- [x] 5.6 为模型页增加统一页面内边距，并让全部设置子页签的头部卡与内容卡保持一致间距和完整四角圆角
- [x] 5.7 审计健康中心、运行时警告、错误、校验、空态、加载/排队/处理状态、Toast 与确认框，统一通过当前界面语言渲染；稳定错误码和警告使用显式映射，未知动态错误只在技术详情保留原文
- [x] 5.8 增加中文/英文提示回归，覆盖 `pyannote.audio`/`HF_TOKEN` 健康警告、语言切换即时重渲染、未知后端错误本地化概括和技术标识不翻译

## 6. 验证与桌面交付

- [x] 6.1 按同一真实桌面视口逐页与产品图对照，记录 P0/P1/P2 及修复前后证据
- [x] 6.2 运行前端单元、类型检查、Web 构建、维护 E2E、后端 Range 回归、视觉回归、前端质量审计和 `npm run check:open-source`
- [x] 6.3 修复最终 DMG 在大型模型存储下同步扫描阻塞事件循环的问题，并覆盖健康检查并发响应测试
- [x] 6.4 构建 v0.3.0 Apple Silicon DMG，执行冻结后端 smoke、外置卷媒体 Range 实测、DMG 校验、挂载内容、架构与 SHA-256 检查
- [x] 6.5 从最终 DMG 启动 Tauri 应用，按 1.3 的矩阵实际触达全部控件并记录结果；任何失败都回到实现阶段
- [x] 6.6 仅在视觉、功能、实机触达和自动化全部通过后归档本 change
