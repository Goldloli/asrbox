## 1. 模型天梯折叠与排序

- [x] 1.1 为模型天梯建立稳定的排序维度与比较规则，覆盖综合、名称、三项等级、四项能力及下载状态；用前端单元或 Playwright 断言证明不同维度会得到预期首行且相同值按名称稳定回退。
- [x] 1.2 把模型天梯改为默认折叠、可访问地展开／收起，并在展开区加入排序选择和中英文文案；更新 `models-download-controls.spec.ts`，验证初始表格隐藏、展开后完整呈现、排序可操作且窄屏只有表格区域横向滚动。

## 2. 首页转写结果精简

- [x] 2.1 将共享字幕工作区拆成首页 `summary` 与任务详情 `detail` 呈现模式，复用现有任务、音频、全文、草稿与下载逻辑；运行 `npm run typecheck` 验证组件契约无类型回退。
- [x] 2.2 在转写首页保留只读全文和既有音频复核，移除字幕预览与分段编辑 DOM，在已完成结果下只显示 SRT/TXT 下载；新增维护中的浏览器场景，验证两个文件内容可下载、其它格式按钮和分段编辑器不出现，失败时仍有错误反馈。

## 3. 任务详情内容优先重构

- [x] 3.1 将任务详情调整为固定标题／状态后依次呈现音频、字幕预览、分段编辑，并把现有元数据、标签、备注、集合、完整格式导出、AI 入口、诊断、版本与任务操作收纳到可发现的次要区域；用 `transcript-editing.spec.ts` 验证打开详情无需切换页签即可编辑、保存、放弃和下载完整格式。
- [x] 3.2 调整抽屉、波形、预览、分段行和工具栏的宽高与滚动边界；在 1280×820、1440×900 和 390×844 验证无页面横向溢出、标题／关闭控制和主要操作可到达，窄屏分段输入不被裁切或底栏遮挡。

## 4. 设计核对与合并门禁

- [x] 4.1 启动本地应用并用 Codex 内置浏览器捕获模型天梯、首页完成结果、桌面任务详情和窄屏任务详情；将图 2／图 3 与实现按相同状态和尺寸组合比较，修复全部 P0/P1/P2，并在仓库根 `design-qa.md` 记录 `final result: passed`。
- [x] 4.2 运行 `npm run typecheck`、`npm run build:web`、相关维护 E2E、`npm run audit:frontend`、`openspec validate --changes streamline-model-task-workspaces` 与 `npm run check:open-source`；复核最终 diff 只包含本 change 与已存在的自动更新 change，准确记录任何只能在 Windows 实机验证的剩余风险。
