## 1. 持久化与提供商基础能力

- [x] 1.1 先为独立 LLM 提供商、校对任务和建议新增失败测试，覆盖来源版本/提供商引用，以及删除任务和提供商时的行为。
- [x] 1.2 新增增量 SQLite migration 和 SQLAlchemy model，并验证既有数据库新增相关表后，不会改变当前任务、分段、版本、ASR 提供商或设置记录。
- [x] 1.3 先新增提供商 service 失败测试，覆盖 6 个指定预设和 custom、凭据脱敏、必需的模型/key 校验、loopback 判定、HTTPS 强制要求及 redirect 拒绝。
- [x] 1.4 使用现有 `requests` 依赖实现 LLM 提供商预设目录、CRUD service、安全 URL 校验、secret 脱敏和经过清理的 Chat Completions 连接测试。

## 2. 字幕校对任务 Service

- [x] 2.1 先新增 service 失败测试，覆盖已完成任务资格、每个任务最多一个活动校对、不可变来源版本输入、确定性分段 batch、只读相邻上下文及重启中断恢复。
- [x] 2.2 实现持久化校对任务创建、隔离的单 worker 执行、进度更新、启动中断恢复及提供商/模型审计快照，同时不改变转写任务状态或 ASR worker 队列。
- [x] 2.3 先新增 parser 测试，覆盖有效的保守建议、过滤未变化文本，以及在 JSON 无效、分段 ID 未知/重复/不属于目标、替换为空或提供商报错时让整个任务失败。
- [x] 2.4 实现固定的保守 prompt、严格的结构化响应解析、经过清理的 LLM 错误映射，以及不保存 prompt 或原始响应的全有或全无建议持久化。

## 3. 原子审阅应用与版本管理

- [x] 3.1 先新增失败测试，覆盖建议默认 `pending`、编辑/后处理/恢复/重新转写后的 `stale` 检测、拒绝空选择或重复应用，以及当前分段原文校验。
- [x] 3.2 重构字幕版本创建，使其支持调用方管理事务，同时保留所有现有转写、重新转写、编辑、后处理和恢复行为。
- [x] 3.3 实现一次性的选择性应用，使分段文本、任务文本、一个 `proofread` 版本、建议处理结果和校对任务状态一同提交或回滚，同时保持时间、说话人、置信度、顺序和边界不变。
- [x] 3.4 扩展任务和提供商删除处理，使校对记录随任务清理，并让历史校对任务的审计字段在 LLM 提供商删除后仍然保留。
- [x] 3.5 验证当前导出包含已应用文本，历史来源版本导出保持不变，并且恢复 `proofread` 版本时会创建现有的可审计 `restore` 版本。

## 4. 带类型的 API Contract

- [x] 4.1 为 LLM 提供商预设/CRUD/测试及校对任务创建/列表/详情/应用新增 Pydantic request/response model 和路由，并为无效、活动中、过期和已应用状态返回明确的 4xx 错误。
- [x] 4.2 新增后端 API 测试，覆盖提供商脱敏与校验、异步任务轮询、无建议和失败状态、持久化审阅、原子应用、过期拒绝、重启恢复及转写失败隔离。
- [x] 4.3 更新 `backend/tests/test_contract.py`，覆盖新增的受维护路由和字段；随后运行 `npm run test:backend:contract`，并检查生成的 FastAPI OpenAPI 中新增 schema 是否准确。

## 5. 前端提供商与审阅流程

- [x] 5.1 为 LLM 提供商预设和校对任务新增准确的 TypeScript API type、client method 和 query key，且不读取内部 `options_json` 字段。
- [x] 5.2 新增独立的 LLM 提供商设置界面，支持 6 个厂商预设、custom 提供商、可编辑 endpoint/model、key 脱敏、启用状态、连接测试，以及基于实际 endpoint 的本地/第三方披露。
- [x] 5.3 在字幕工作台中新增已完成任务的校对流程，包括提供商选择、运行前数据披露、`queued`/`running`/`failed`/`interrupted`/无建议状态、轮询和持久化任务历史。
- [x] 5.4 新增建议审阅界面，使用纯文本渲染原文/建议/原因，默认不选中，支持单选/全选、`stale` 和本地编辑保护、一次应用确认，并在成功后刷新 task/version/run query。
- [x] 5.5 新增中英文文案及聚焦的 component 或 Playwright 覆盖，验证提供商设置、云端与 loopback 披露、审阅选择、过期拒绝、响应式布局、键盘焦点和成功应用。

## 6. 隐私、备份与验证

- [x] 6.1 更新项目负责维护的隐私和用户文档，准确说明 LLM 校对会发送什么、如何判定 loopback、如何存储 key/run/suggestion、备份包含什么，以及日志和诊断会排除什么。
- [x] 6.2 新增回归覆盖，证明备份会保留新增的 SQLite 数据，而诊断包会排除 LLM key、prompt、字幕 payload、原始响应和建议文本。
- [x] 6.3 迭代期间运行聚焦后端测试；准备完成后运行 `npm run test:backend`、`npm run test:backend:contract`、`npm run typecheck`、`npm run build:web`、相关 Playwright 场景和 `npm run check:open-source`。
- [x] 6.4 根据本 change 的目标与非目标审阅最终实现 diff，确认现有转写/版本/恢复/导出行为保持不变，并在 archive 前对齐代码、测试、文档和 delta spec。

## 7. 后端韧性回归覆盖

- [x] 7.1 用真实的旧数据库 migration 测试替换 schema smoke test：从不存在 LLM 校对表的数据库开始，并保留既有 ASR 提供商、任务和字幕版本记录。
- [x] 7.2 新增双 session 并发测试，证明同时创建校对任务只会留下一个活动任务，同时应用建议只会创建一个 `proofread` 版本。
- [x] 7.3 新增进程级后端启动测试，证明 `queued`/`running` 校对任务会变为 `interrupted`，同时已完成校对任务、转写任务状态、字幕文本和现有版本保持不变。

## 8. AI 工作区信息架构

- [x] 8.1 新增桌面侧栏、移动端底部导航和 `/ai` 路由，并在窄屏验证第五个导航项及纵向工作区不会溢出或重叠。
- [x] 8.2 实现左侧可核对任务列表，只显示已完成且有字幕版本的任务，支持最近完成排序、文件名搜索、任务元数据和区分后的空状态。
- [x] 8.3 从任务详情移除完整校对面板，只为成功字幕保留“AI 字幕核对”轻入口，并通过路由参数在 AI 工作区自动选中任务。
- [x] 8.4 实现右侧字幕核对工作区，包含上次成功提供商选择、首次默认提供商、非阻塞隐私提示、后台轮询、最近结果和折叠的历史核对。

## 9. 审阅结果与恢复反馈

- [x] 9.1 按来源版本顺序组合建议和无建议字幕，将每个连续无建议区间独立折叠，并显示数量、时间范围和按需展开内容。
- [x] 9.2 展示建议时间、纯文本原文/建议/原因和差异强调，保持默认不选，支持单选、全选、取消全选，并禁止内联编辑建议。
- [x] 9.3 实现一次应用确认、应用后原页成功状态、“查看新字幕”和“重新核对”，并保持过期、已应用和历史结果只读语义准确。
- [x] 9.4 为无建议、鉴权、限流、超时、连接、模型/HTTP、上下文过长、无效响应、结构化错误、提供商缺失、中断和过期状态提供分类文案、恢复操作与折叠错误详情。

## 10. AI LLM 提供商设置

- [x] 10.1 将 LLM 提供商管理迁移到设置页一级 `AI LLM提供商` tab，现有 ASR 在线提供商页面恢复原有边界。
- [x] 10.2 完善预设与自定义配置、遮罩凭据、启停和删除流程；保存后自动测试，并在每张卡片保留带加载、成功和分类失败反馈的“测试连接”操作。

## 11. 批处理、测试与最终验证

- [x] 11.1 先新增后端失败测试，再把校对 batch 上限调整为 500 个目标分段或 30,000 个字幕字符，并增加明确的上下文过长错误映射。
- [x] 11.2 更新 API/contract、组件和 Playwright 覆盖，验证独立 AI 路由、任务资格、连续区间折叠、提供商测试、错误恢复、一次应用和桌面/移动响应式行为。
- [x] 11.3 运行聚焦后端测试、`npm run test:backend:contract`、`npm run typecheck`、`npm run build:web`、相关 Playwright 场景和 `npm run check:open-source`，并审阅最终 diff 与 OpenSpec 一致性。
