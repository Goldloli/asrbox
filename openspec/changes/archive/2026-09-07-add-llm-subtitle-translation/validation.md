# 验证记录

验证于 2026-09-06 至 2026-09-07 在 `codex/llm-subtitle-translation` 完成。功能仍属未发布开发分支，未 commit、push 或构建发布包。

## 自动化验证

- `npm run check:open-source`：通过。最终运行使用项目默认 Chromium 配置，包含依赖完整性、版本检查、发布工具测试、第三方文件校验、TypeScript、前端单测、Web 构建、后端全套、Cargo 和维护 Playwright 聚合。
- 后端全套：326 passed，包含旧校对、原文编辑／导出和新增翻译测试。
- `npm run test:backend:contract`：9 passed，断言准确路由、JSON response_model 和实际返回字段；翻译下载暴露 Content-Disposition，供桌面跨源前端读取安全文件名。
- 前端单测：15 passed；维护 Playwright：31 passed，其中 7 个为新增翻译场景；Rust 测试：18 passed。
- 新增翻译验证覆盖：旧库与中断迁移、语言校验、完整来源预检、批次边界、严格响应结构、限流／超时／响应上限、检查点续译、取消迟到、重复 worker、线程池响应、删除竞争、备份恢复、完整版本原子提交、保存冲突、六格式转义和明确版本导出。
- 续译使用创建时持久化的批次成员关系，已验证默认批次大小变化后不会重新分配旧批次。
- 浏览器验证覆盖：旧核对入口、新任务／run 定位、刷新、保存与冲突草稿、取消／恢复、提供商配置变更、任务切换隔离、已活动来源只读历史、RTL 与窄屏、真实下载，以及合成 WAV 的播放定位与文件缺失。
- `git diff --check`：通过。已有模型 CPU／CUDA／MPS／MLX 展示修改保留，原有维护测试均通过。未修改 ASR 执行器、发行版本、依赖清单或发布资产。

## 真实本地 LLM 与 Docker

使用本机已安装的 Ollama `qwen3-vl:8b`，仅发送合成短字幕，无真实用户媒体、字幕或付费远端调用。

| 语言对 | 输入示例 | 实际输出示例 | 结果 |
| --- | --- | --- | --- |
| 日语 → 法语 | 今日は良い天気です。 | Aujourd'hui, il fait beau. | 两段全部完成，生成完整版本及六格式文件 |
| 英日混合 → 阿拉伯语 | 列車は五分後に出発します。 | سيغادر القطار بعد خمس دقائق. | 自动源语言，两段全部完成 |
| 繁体 → 简体 | 這部影片介紹城市交通。 | 这部影片介绍城市交通。 | 两段全部完成，保留原文 |

在隔离的 `asrbox:local` Docker 容器中，挂载当前后端和 Web 构建，使用独立临时数据卷、loopback 端口及合成静音 WAV。通过真实网页完成日语→法语翻译、第二段播放定位、人工修订、保存新版本、实际下载双语 SRT、刷新后恢复运行和已保存译文。下载中原日语与修订法语逐段对应，原时间轴保持不变。该验证不是重新构建发行镜像或执行完整 Docker 发布门禁。

## 明确未验证的范围

- macOS 原生开发版通过 Cargo 编译并进入 `target/debug/asrbox` 启动阶段，使用隔离应用标识；界面工具无法识别这个未打包的开发进程（按可执行文件路径及隔离 bundle identifier 均返回 Invalid app）。因此未完成原生窗口逐项人工操作，尤其是系统保存对话框；不把 Web 或 mock 场景当作这部分已验证。
- 未构建 DMG／签名／公证，也未执行真实 ASR 模型或长音频推理；本变更不涉及这些执行与打包路径。
- 未调用任何付费远端 LLM。实际语言质量只验证了上述模型与合成短文本，结构校验不证明任意语种的语义准确性或长篇术语一致性。
