## 1. 建立回归基线

- [x] 1.1 审阅当前工作树中已有的首页、任务详情、模型天梯和自动更新改动，记录本 change 可修改的前端接触点并确认不会覆盖独立 change；以 `git diff --check` 和按文件 diff 审阅结果验证边界清晰
- [x] 1.2 为专业视觉系统新增或扩展维护中的 Playwright 场景，先覆盖五个主路由、任务中心三栏、播放联动和设置全部子页签的可达性、主题标识及页面级横向溢出，并将场景接入 `test:e2e:maintained` 聚合命令；以聚焦场景能够在重构前暴露至少一项预期差异验证测试有效
- [x] 1.3 为侧边栏默认值、已有偏好保留、system/light/dark 主题映射和强调色持久化／导入导出补充前端单元测试；以 `npm run test:frontend:unit` 通过验证状态 contract
- [x] 1.4 为默认模型从模型管理、设置到首页的双向联动补充前端或浏览器回归场景，覆盖当前草稿手动选择不被覆盖；以聚焦测试在实现前失败、实现后通过验证 contract

## 2. 统一视觉 token 与共享结构

- [ ] 2.1 重构 `app/src/index.css` 的画布／表面、文字／边框、强调／语义状态和尺寸／动效 token，增加橙、蓝、紫、粉、红、绿、青、灰的亮暗主题强调色映射，并移除造成卡片堆叠与主题割裂的全局效果；以逐色亮暗截图及对比度检查验证连续表面与固定语义状态色
- [ ] 2.2 扩展现有 `surfaces.tsx` 和 `app/src/components/ui/`，提供页面标题、轻量内容分区、分隔行列表、工作区侧栏、上下文检查器和统一反馈条变体，同时保持既有组件调用兼容；以组件单元测试和 `npm run typecheck` 验证
- [ ] 2.3 统一 `weiui.tsx` 及共享按钮、输入、选择、切换、标签、菜单和弹层的高度、圆角、禁用态、状态色与焦点环，确保 Select 空态继续使用非空哨兵值；以键盘聚焦测试和相关前端单元测试验证
- [ ] 2.4 为展开、侧栏位移、状态变化和主题切换增加克制的过渡并完整支持 `prefers-reduced-motion`；以浏览器媒体偏好场景确认减少动效时不播放非必要过渡

## 3. 重构应用壳体与导航

- [x] 3.1 调整 `AppShell.tsx`、`GlobalSearch.tsx` 与相关状态区，形成稳定侧边导航、顶部命令区、连续主画布和轻量底部任务状态，且为持久播放器与任务中心预留空间；以桌面和 390px 场景确认主内容及最后一行操作不被遮挡
- [x] 3.2 更新 `Sidebar.tsx` 与 `uiStore.ts`，让无持久化偏好的新用户默认显示带文字侧栏，同时保留已有用户的展开／仅图标偏好和切换能力；以 1.3 的单元测试及浏览器持久化场景验证
- [x] 3.3 统一 `MobileNav.tsx`、`TaskCenterDrawer.tsx` 和 `PersistentAudioPlayer.tsx` 的新视觉语言与响应式边界；以窄屏导航、任务中心打开和播放器显示场景确认无覆盖、截断及页面级横向滚动

## 4. 重构转写首页

- [x] 4.1 将 `TranscribePage.tsx` 的文件选择、模型、语言、更多设置和开始转写动作组织为单一创建工作区，复用现有业务状态与 mutation；以文件选择、参数设置和启动转写的维护场景验证行为未变
- [x] 4.2 将最近任务迁移为桌面可扫描、窄屏可重排的分隔行列表，保留状态、进度、常用操作、空态、离线态和错误恢复；以长文件名、进行中、失败和空列表场景验证布局与操作可达
- [x] 4.3 统一首页运行中与完成结果的表面和反馈，确认完成结果下方仅保留 SRT 与 TXT 下载且成功／失败提示可诊断；以现有转写编辑场景和聚焦下载场景验证

## 5. 重构任务列表与任务详情

- [x] 5.1 将 `TasksPage.tsx` 重构为常驻三栏任务中心：左栏只含筛选与任务列表且无导入，选择行直接驱动中央与右栏；以任务选择、长文件名和桌面列表场景验证
- [x] 5.2 在中央主区组合任务标题、音频播放器／波形、转写结果、分段编辑和保存／放弃操作，不重复创建页的转写设置；以任务切换、详情滚动和字幕编辑场景验证
- [x] 5.3 实现播放器、波形、时间显示和当前转写分段的双向联动，覆盖播放跨段、点击分段定位、波形 seek、自动滚入可见区和编辑焦点保护；以媒体事件单元测试及聚焦 Playwright 场景验证
- [x] 5.4 将右栏限定为任务状态、时间、时长、大小、格式、导出，以及可折叠的标签备注、版本诊断、重试、清理和删除，确保不存在模型／语言／说话人设置；以内容断言和现有危险操作场景验证
- [x] 5.5 为中等和窄屏实现任务列表与状态栏的折叠、切换或堆叠规则，保持音频、转写结果、分段编辑和保存／放弃优先可达；以 390px 场景验证无页面级横向滚动且键盘顺序合理

## 6. 重构 AI 与模型页签

- [x] 6.1 将 `AIPage.tsx` 的聊天、校对、翻译及任务选择统一为共享页头、模式工具栏、列表／编辑工作区和反馈位置，保持流式响应与既有操作 contract；以 `llm-proofreading`、`llm-translation` 和 `chat` 维护场景验证
- [x] 6.2 将 `ModelsPage.tsx` 的模型天梯、目录、推荐、下载、设备能力和存储入口迁移到统一分区与分隔行模式，保持天梯默认折叠及各列排序；以模型天梯排序、下载控制和存储设置维护场景验证
- [x] 6.3 验证模型天梯展开后的多列表格只在自身区域滚动，并在亮暗主题及 390px 宽度保持排序控件可操作；以聚焦 Playwright 响应式场景验证
- [x] 6.4 让模型管理“设为默认”与设置默认模型复用同一 ASR 设置更新并集中刷新相关 query，首页仅在新建／重置或未手动覆盖时吸收默认值；以 1.4 的联动场景验证三处一致
- [x] 6.5 盘点并接入 Ollama、Qwen、OpenAI 等已知项的正确官方品牌图标，为未知项保留通用图标并记录资源来源／许可；以亮暗主题视觉检查和资源／notice 审计验证

## 7. 重构设置及全部子页签

- [x] 7.1 将 `SettingsPage.tsx` 的顶层页签和通用设置分组迁移为统一页头、页签栏及说明／控件响应式布局，保留主题、密度、字体、侧边栏和转写默认值；以设置持久化及 `transcription-defaults` 场景验证
- [x] 7.2 在通用设置增加橙色默认的固定强调色色板，支持蓝、紫、粉、红、绿、青、灰并同步设置导入／导出，保证成功／错误等语义色不随选择变化；以 1.3 的单元测试和逐色浏览器检查验证
- [x] 7.3 将提供商、模型存储、CUDA／加速、更新、隐私／关于等全部现有设置子页签迁移到同一分组、表单、状态和反馈模式，减少嵌套卡片但不删除字段或诊断；以 `llm-provider-models`、`model-storage-settings`、`cuda-acceleration` 和 `about-updates` 维护场景验证
- [x] 7.4 检查 `ProvidersPage.tsx` 的重定向与兼容入口在新壳体下保持一致，并验证直接访问旧入口仍进入正确设置页签

## 8. 文案、可访问性与视觉验收

- [x] 8.1 更新受影响的中英文 i18n 文案，使页面标题、操作、空态、错误与状态反馈在全部页签中使用一致产品语言；以 i18n key 检查、`npm run typecheck` 和中英文浏览器抽查验证
- [x] 8.2 对导航、搜索、页签、表单、菜单、详情侧区和危险操作执行键盘与可访问名称检查，并修复亮暗主题焦点与对比度问题；以维护中的键盘场景和浏览器可访问性人工检查验证
- [x] 8.3 采集亮色首页、亮色任务详情、同构暗色首页，以及 AI、模型、设置代表页面的桌面／窄屏截图，与用户选定方向比较并记录视觉 QA；以无主题割裂、无遮挡、无截断、无页面级横向溢出作为通过条件

## 9. 集成验证与归档

- [x] 9.1 运行 `npm run test:frontend:unit`、`npm run typecheck`、`npm run build:web`、`npm run audit:frontend` 和相关聚焦 Playwright 场景，修复所有由本 change 引入的失败
- [x] 9.2 运行完整 `npm run test:e2e:maintained`，确认自动更新、模型管理、字幕编辑、AI 和设置等相邻工作流未回归
- [x] 9.3 审阅最终 diff 与独立自动更新 change 的边界，运行 `git diff --check` 和 `npm run check:open-source`，准确记录任何因环境限制未执行的检查
- [x] 9.4 在全部功能与回归通过后运行 `npm run version:bump -- 0.3.0`，再运行 `npm run check:versions` 验证根 workspace、app、web、Tauri、后端和 lockfile 的版本一致；不得在本任务中 tag 或发布
- [x] 9.5 更新受影响的 README／用户文档或产品截图，勾选全部已完成任务，运行 `openspec validate --changes unify-professional-ui-system`，随后按项目流程同步并归档 change，再以 `openspec list --json` 和 `openspec validate --specs` 确认主规格有效
