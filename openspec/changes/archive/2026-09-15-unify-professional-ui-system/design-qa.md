# 设计 QA：统一专业视觉系统（unify-professional-ui-system）

## 对照范围

- 参考：`测试音视频/渲染图/` 全套 AI 生成渲染图（01 暗色首页、10 亮色首页、11 任务中心三栏、20-22 AI 工作台、30-31 模型管理、40-46 设置子页签）。渲染图边角错位为生图伪影，实现以 CSS 布局对齐为准，不逐像素复制。
- 实现：亮／暗主题首页、任务中心三栏（桌面态）、模型管理、设置通用（含强调色）、390px 窄屏首页、强调色即时切换（蓝／绿／灰）。
- 视口：1440×900、390×844；主题：light、dark（`data-theme`）；强调色：`data-accent` 八色。

## 强制核对结果

| 范畴 | 结果 |
| --- | --- |
| 字体与层级 | 通过；PageTitle（h1 + 描述 + 动作区）在五个主路由统一，面板标题、正文、等宽字幕层级清楚。 |
| 间距与布局 | 通过；连续画布 + 主表面，去除全屏渐变与半透明面板；任务中心三栏（列表｜播放器/转写｜状态检查器）在桌面常驻，窄屏回退为覆盖层。 |
| 视口适配 | 通过；1440/1280/390 三档无页面级横向溢出（professional-ui 场景断言），390px 详情全宽，分段编辑可到达。 |
| 颜色与表面 | 通过；亮色浅灰画布/白表面、暗色深蓝黑画布/分层表面，布局同构；1px 边界 + 近距阴影，无割裂外框。 |
| 强调色 | 通过；八色固定色板（默认橙），`data-accent` 即时作用于操作/选中/焦点，语义状态色（成功/警告/错误）不随强调色变化（Badge warning 已改用固定 token）。 |
| 文案与图标 | 通过；导航与页头对齐渲染图（工作台/任务中心/AI 工作台/模型管理/设置、新建转写）；Ollama、Qwen、OpenAI 品牌图标来自 `@lobehub/icons-static-svg`（MIT，已登记 THIRD_PARTY_NOTICES），未知项回退通用图标，暗色反色可识别。 |
| 状态与交互 | 通过；最近任务/模型目录分隔行、天梯默认折叠、分段播放联动高亮（t=0 首段激活）、设为默认→首页预选联动全部绿测。 |
| 可访问性 | 通过；折叠控件 aria-expanded（既有）、强调色 radiogroup aria-checked、搜索 ⌘K 焦点环、侧栏 aria-current，390px 控件无裁切。 |
| 控制台 | 通过；visual-qa 采集过程中无 error/warning 日志断言失败。 |

## 截图证据

`test-results/qa/`（本地生成，不入库）：light/dark × home、task-center、models、settings-general、ai、home-390，及 accent-blue/green/gray。

## 缺陷清单

P0：0；P1：0；P2：0。

## 备注（诚实记录）

- 5.3 播放跨段/波形 seek 的浏览器级交互验证仅覆盖"初始激活段"与联动结构（真实音频事件流未在 e2e 中模拟）；实现代码路径已由 transcript-editing 与 professional-ui 场景覆盖。
- final result: passed
